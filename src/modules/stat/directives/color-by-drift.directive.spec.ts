import { ColorByDriftDirective } from './color-by-drift.directive';

describe('ColorByDriftDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorByDriftDirective();
    expect(directive).toBeTruthy();
  });
});
