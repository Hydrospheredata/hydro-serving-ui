import { CommonModule } from '@angular/common';
import { NgModule } from '@angular/core';
// import { StoreModule } from '@ngrx/store';
// import { reducers } from '@form/reducers';

@NgModule({
    imports: [
        CommonModule,
        // StoreModule.forFeature('form', reducers),
    ],
    declarations: [],
})
export class FormModule { }
