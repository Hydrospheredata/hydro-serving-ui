import { SidebarFilterPipe } from './sidebar-filter.pipe';

describe('SidebarFilterPipe', () => {
    it('create an instance', () => {
        const pipe = new SidebarFilterPipe();
        expect(pipe).toBeTruthy();
    });
});
